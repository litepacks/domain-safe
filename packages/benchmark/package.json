{
  "name": "@domain-safe/benchmark",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "benchmark": "node dist/benchmark.js",
    "build": "tsup",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@domain-safe/core": "workspace:*"
  },
  "devDependencies": {
    "tsup": "^8.3.5"
  }
}
